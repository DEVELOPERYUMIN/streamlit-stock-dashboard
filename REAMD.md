* streamlit 
'파이썬 스크립트를 웹앱처럼 실행' 하는 프레임 워크

동작원리 : 스크립트를 전체 재실행
사용자가 입력을 바꾸거나, 버튼을 누르거나, 위젯 상태가 변하면, 파이썬 파일이 
위에서 아래로 통째로 다시 실행됨 

보편적인 스타일
위쪽 : 입력 위젯들
안쪽 : 조건문 (if:) 안에서 결과 보여줌 

위젯은 상태(state)를 가짐 

화면에 뿌리는 건  print() 가 아니라 st.xxx()
ex ) st.write(), st.dataframe(), st.image